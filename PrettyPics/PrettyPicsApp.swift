//
//  PrettyPicsApp.swift
//  PrettyPics
//
//  Created by PJ Loury on 12/1/24.
//

// Add a progress indicator
// Filter for nature/scenery
// Add a cache for between sessions– remember the photos that have been evaluated previously, make the assessment in a way where tuning the parameters doesn't require needing to evaluate the photo again if possible
// Output: Create an album within photos
// Point to a directory and scan all subdirectories, Only look at photos
// Output: Create a folder with aliases to all photos

// Assessments
// People should have their eyes open

import SwiftUI

@main
struct PrettyPicsApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
